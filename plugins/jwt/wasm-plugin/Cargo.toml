[package]
name = "jwt-plugin"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
wit-bindgen = "0.28.0"

[dev-dependencies]
expectest = "0.12.0"

[build]
target = "wasm32-wasi"
